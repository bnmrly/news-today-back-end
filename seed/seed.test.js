// check on this tomorrow

// process.env.NODE_ENV = 'test';
// const db_url = require('../config')[process.env.NODE_ENV];
// const mongoose = require('mongoose');
// const seedDB = require('./seed');

// mongoose
//   .connect(db_url)
//   .then(() => {
//     return seedDB();
//   })
//   .then(() => {
//     mongoose.disconnect();
//   });

// put a catch in at the end .catch (err) console.log err
// i need to return  mongoose.disconnect();
